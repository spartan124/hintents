name: Integration Tests – Cross-Platform Binary

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# ─────────────────────────────────────────────────────────────────────────────
# Matrix: Linux · macOS Intel · macOS Apple Silicon · Windows
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  integration:
    name: Integration / ${{ matrix.os }}
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false          # run all platforms even if one fails
      matrix:
        include:
          - os: Linux
            runner: ubuntu-24.04
            binary_suffix: ""
            go_arch: amd64
            race_flag: "-race"
            timeout: "120s"

          - os: macOS-Intel
            runner: macos-latest     # Intel runner (fallback to latest if 13 fails)
            binary_suffix: ""
            go_arch: amd64
            race_flag: "-race"
            timeout: "120s"

          - os: macOS-Apple-Silicon
            runner: macos-latest     # M-series ARM runner
            binary_suffix: ""
            go_arch: arm64
            race_flag: "-race"
            timeout: "120s"

          - os: Windows
            runner: windows-2022
            binary_suffix: ".exe"
            go_arch: amd64
            race_flag: ""           # Race detector disabled on Windows (known flakiness)
            timeout: "180s"         # Longer timeout to account for Windows overhead

    env:
      # Allow the test suite to find the compiled binary without hard-coding a path.
      ERST_BINARY: ${{ github.workspace }}/erst${{ matrix.binary_suffix }}

    defaults:
      run:
        shell: bash   # Use bash on all platforms for consistent path handling

    steps:
      # ── Checkout ──────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── Go toolchain ──────────────────────────────────────────────────────
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # ── Rust toolchain (for the Soroban simulator) ─────────────────────────
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache Rust build artefacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: simulator -> target

      # ── Build erst binary ──────────────────────────────────────────────────
      - name: Build erst (Go CLI)
        run: |
          go build -v -o "${{ env.ERST_BINARY }}" ./cmd/erst

      # ── Build Rust simulator ───────────────────────────────────────────────
      - name: Build erst-sim (Rust simulator)
        working-directory: simulator
        run: cargo build --release

      # ── Validate binary was produced ───────────────────────────────────────
      - name: Validate binary exists
        run: |
          if [ ! -f "${ERST_BINARY}" ]; then
            echo "ERROR: Binary not found at ${ERST_BINARY}"
            ls -la "$(dirname "${ERST_BINARY}")" || true
            exit 1
          fi
          echo "Binary confirmed at: ${ERST_BINARY}"

      # ── Verify binary is executable ────────────────────────────────────────
      - name: Smoke-test binary (--version)
        run: |
          "${{ env.ERST_BINARY }}" --version

      # ── Windows diagnostic info (aids debugging on failures) ───────────────
      - name: Windows diagnostic info
        if: runner.os == 'Windows'
        run: |
          echo "ERST_BINARY=${ERST_BINARY}"
          echo "GOARCH=${{ matrix.go_arch }}"
          echo "race_flag=${{ matrix.race_flag }}"
          go env GOOS GOARCH
          ls -la "${ERST_BINARY}"

      # ── Unit tests (fast gate before integration) ─────────────────────────
      - name: Run Go unit tests
        run: go test -v ${{ matrix.race_flag }} ./...

      # ── Integration test suite ─────────────────────────────────────────────
      - name: Run integration tests
        run: |
          go test -v ${{ matrix.race_flag }} -timeout ${{ matrix.timeout }} ./integration/...
        env:
          ERST_BINARY: ${{ env.ERST_BINARY }}

      # ── Upload test results on failure ────────────────────────────────────
      - name: Upload test logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs-${{ matrix.os }}
          path: |
            **/*.log
            **/test-output.txt
          retention-days: 7

  # ─────────────────────────────────────────────────────────────────────────
  # Summary job – required status check for branch protection
  # ─────────────────────────────────────────────────────────────────────────
  integration-complete:
    name: Integration complete
    runs-on: ubuntu-24.04
    needs: integration
    if: always()
    steps:
      - name: Check all matrix jobs passed
        run: |
          if [ "${{ needs.integration.result }}" != "success" ]; then
            echo "One or more platform integration jobs failed."
            exit 1
          fi
          echo "All platform integration jobs passed."